require('mocha-cakes-2');
require('chai').should();

Feature('View Whitelist List', () => {
  Scenario('empty whitelist', () => {
    Given('a user has logged in', () => {

    });

    And('no whitelist is added to the user\'s carrier before', () => {

    });

    When('a user go to User Activation page', () => {

    });

    Then('the user should see an empty table', () => {

    });

    And('the user should not be able to click on any pagination buttons', () => {

    });

    And('the user should be able to select records to be shown on the page', () => {

    });
  });
});
